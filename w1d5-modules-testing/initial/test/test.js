// Your test code goes here!

// We need to require something that allows us to verify results
// Here were using the `assert` package, but we could also use `chai`
var assert = require('assert');

describe('Description of what is being tested', function() {
  it('should do something very specific', function() {
    assert.equal(true, true);
  });
});
